# Mock API Server - Визуальный Mock-сервер и Песочница API

Мощный инструмент для создания и управления HTTP моками с удобным веб-интерфейсом. Создавайте моки визуально, импортируйте из Postman, OpenAPI/Swagger или curl команд, настраивайте задержки, кэширование, ошибки и прокси — всё без редактирования кода.

## 🚀 Основные возможности

- **Визуальное создание моков** — создавайте HTTP моки через удобный веб-интерфейс
- **Иерархическая организация** — группируйте моки по папкам и подпапкам
- **Импорт из различных источников**:
  - Postman Collection v2.1
  - OpenAPI 3.x / Swagger 2.0 (JSON/YAML)
  - curl команды
- **Гибкое сопоставление запросов**:
  - Поддержка всех HTTP методов (GET, POST, PUT, DELETE, PATCH, HEAD, OPTIONS)
  - Умные заголовки (обязательные/необязательные)
  - Проверка содержимого тела запроса
- **Расширенные возможности**:
  - Задержки ответа (фиксированные и случайные из диапазона)
  - Кэширование ответов
  - Имитация ошибок с вероятностью
  - Проксирование запросов на реальный backend
  - Rate limiting
- **Мониторинг и аналитика**:
  - История всех запросов
  - Метрики Prometheus
  - Логирование в JSON формате
- **Управление папками**:
  - Создание, переименование, удаление папок и подпапок
  - Дублирование папок с моками
  - Drag-and-drop для изменения порядка

## 📋 Требования

- **Python 3.8+**
- **PostgreSQL 12+**
- **Node.js 16+** (для фронтенда)
- **npm** или **yarn**

## 🛠️ Установка и развертывание

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd mockl-test
```

### 2. Настройка базы данных PostgreSQL

Создайте базу данных:

```sql
CREATE DATABASE mockl;
```

Или используйте существующую базу данных.

### 3. Настройка переменных окружения

#### Backend

Скопируйте файл с примерами переменных окружения:

```bash
cp backend/.env.example backend/.env
```

Отредактируйте `backend/.env` и заполните необходимые значения:

```env
# Обязательные параметры базы данных
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mockl
DB_USER=postgres
DB_PASS=your_password_here

# Опциональные настройки
MOCKL_LOG_LEVEL=INFO
MOCKL_DEFAULT_CACHE_TTL=0
MOCKL_RATE_LIMIT_REQUESTS=0
MOCKL_RATE_LIMIT_WINDOW_SECONDS=60
MOCKL_MAX_REQUEST_BODY_BYTES=0
MOCKL_ALLOWED_PROXY_HOSTS=
MOCKL_OPENAPI_SPECS_DIR=
MOCKL_OPENAPI_SPECS_URLS=
```

Полное описание всех переменных окружения см. в файле `backend/.env.example`.

#### Frontend

Создайте файл `frontend/.env` (опционально):

```env
VITE_BACKEND_URL=http://localhost:8000
```

### 4. Установка зависимостей

#### Backend

```bash
cd backend
pip install -r requirements.txt
```

#### Frontend

```bash
cd frontend
npm install
```

### 5. Запуск приложения

#### Разработка

**Backend:**

```bash
cd backend
uvicorn app:app --reload --host 0.0.0.0 --port 8000
```

**Frontend:**

```bash
cd frontend
npm run dev
```

Приложение будет доступно по адресу:
- Frontend: http://localhost:5173 (или другой порт, указанный Vite)
- Backend API: http://localhost:8000
- API документация: http://localhost:8000/docs

#### Production

**Backend:**

```bash
cd backend
uvicorn app:app --host 0.0.0.0 --port 8000 --workers 4
```

**Frontend:**

```bash
cd frontend
npm run build
```

Собранные файлы будут в директории `frontend/dist`. Настройте веб-сервер (nginx, Apache) для раздачи статических файлов.

### 6. Развертывание на Render.com

Проект включает конфигурацию `backend/render.yaml` для автоматического развертывания на Render.com.

1. Подключите репозиторий к Render.com
2. Создайте PostgreSQL базу данных в Render.com
3. Настройте переменные окружения в настройках сервиса
4. Render автоматически развернет приложение

## 📚 Использование

### Базовый сценарий

1. Откройте веб-интерфейс в браузере
2. В поле «Бэк» укажите URL работающего backend-сервера (опционально)
3. Создайте папку (страницу) для логической группы моков
4. Нажмите «Создать mock» или используйте импорт
5. Укажите метод и путь эндпоинта
6. Настройте условия запроса (заголовки, тело)
7. Настройте ответ (статус, заголовки, тело)
8. Сохраните мок и убедитесь, что он активен
9. Используйте URL из «Базовый URL этой страницы» + путь для вызова мока

### Импорт моков

#### Из curl команды

В форме создания/редактирования мока:
1. Вставьте curl команду в поле «Импорт из curl»
2. Или нажмите «Из буфера» для автоматического извлечения из буфера обмена
3. Система автоматически извлечёт метод, URL, заголовки и тело запроса

#### Из Postman Collection

1. Нажмите кнопку «Импорт» в верхней панели
2. Выберите файл Postman Collection v2.1
3. Система автоматически создаст папку и моки по всем запросам

#### Из OpenAPI/Swagger

1. Нажмите кнопку «Импорт OpenAPI/Swagger» в верхней панели
2. Вставьте URL до спецификации или загрузите файл
3. Система поддерживает OpenAPI 3.x и Swagger 2.0
4. Автоматически извлекаются примеры запросов и ответов
5. Если примеры не указаны, система генерирует их из схем

### Расширенные возможности

#### Задержки ответа

**Фиксированная задержка:**
- Укажите значение в поле «Задержка (мс)» в форме мока

**Случайная задержка из диапазона:**
Добавьте в тело ответа специальный блок:

```json
{
  "data": {
    "message": "Успешный ответ"
  },
  "__delay_range_ms__": {
    "min": 100,
    "max": 500
  }
}
```

#### Имитация ошибок

Добавьте в тело ответа специальный блок:

```json
{
  "data": {
    "message": "Успешный ответ"
  },
  "__error_simulation__": {
    "probability": 0.3,
    "status_code": 500,
    "delay_ms": 100,
    "body": {
      "error": "Internal Server Error",
      "message": "Произошла случайная ошибка"
    }
  }
}
```

#### Кэширование

Включите опцию «Включить кэширование ответа» и укажите TTL в секундах.

#### Проксирование

Настройте прокси для папки:
1. Выберите папку
2. Настройте прокси в настройках папки
3. Укажите базовый URL реального backend
4. Запросы без моков будут проксироваться на реальный сервер

## 🔧 API Endpoints

### Основные эндпоинты

- `GET /healthz` — Health check
- `GET /readyz` — Readiness check
- `GET /info` — Информация о сервере
- `GET /docs` — Swagger UI документация
- `GET /metrics` — Метрики Prometheus

### Управление папками

- `GET /api/folders` — Список всех папок
- `POST /api/folders` — Создать папку
- `DELETE /api/folders?name={name}` — Удалить папку
- `PATCH /api/folders/{name}/rename` — Переименовать папку
- `POST /api/folders/duplicate` — Дублировать папку

### Управление моками

- `GET /api/mocks?folder={folder}` — Список моков в папке
- `POST /api/mocks` — Создать мок
- `PUT /api/mocks/{id}` — Обновить мок
- `DELETE /api/mocks/{id}` — Удалить мок
- `POST /api/mocks/{id}/clone` — Клонировать мок

### Импорт

- `POST /api/import/postman` — Импорт из Postman Collection
- `POST /api/import/openapi` — Импорт из OpenAPI/Swagger

### История и метрики

- `GET /api/request-logs?folder={folder}` — История запросов папки
- `GET /api/request-logs/global` — Глобальная история запросов
- `GET /api/metrics?folder={folder}` — Метрики папки
- `GET /api/metrics/global` — Глобальные метрики

Полная документация API доступна по адресу `/docs` после запуска сервера.

## 🔐 Безопасность

- Настройте `MOCKL_ALLOWED_PROXY_HOSTS` для ограничения проксирования
- Используйте `MOCKL_MAX_REQUEST_BODY_BYTES` для ограничения размера запросов
- Настройте rate limiting через `MOCKL_RATE_LIMIT_REQUESTS`
- В production используйте HTTPS
- Ограничьте доступ к API через firewall или reverse proxy

## 📊 Мониторинг

### Метрики Prometheus

Метрики доступны по адресу `/metrics`:

- `mockl_requests_total` — Общее количество запросов
- `mockl_mock_hits_total` — Количество совпадений с моками
- `mockl_proxy_requests_total` — Количество проксированных запросов
- `mockl_response_time_seconds` — Время ответа

### Логирование

Логи выводятся в JSON формате для удобной интеграции с системами мониторинга:

```json
{
  "timestamp": "2024-01-01T12:00:00Z",
  "level": "INFO",
  "logger": "mockl",
  "message": "Request processed"
}
```

Уровень логирования настраивается через переменную `MOCKL_LOG_LEVEL`.

## 🐛 Устранение неполадок

### Проблемы с подключением к БД

1. Проверьте, что PostgreSQL запущен
2. Убедитесь, что переменные окружения настроены правильно
3. Проверьте доступность БД: `psql -h DB_HOST -U DB_USER -d DB_NAME`

### Проблемы с миграциями

При первом запуске система автоматически создаст необходимые таблицы и выполнит миграции. Если возникли проблемы:

1. Проверьте логи сервера
2. Убедитесь, что у пользователя БД есть права на создание таблиц
3. Проверьте, что база данных существует

### Проблемы с импортом

- **Postman Collection**: Убедитесь, что используется формат v2.1
- **OpenAPI**: Проверьте валидность спецификации
- **curl**: Убедитесь, что команда корректна

## 📝 Лицензия

[Укажите лицензию]

## 🤝 Вклад в проект

[Инструкции по внесению вклада]

## 📞 Поддержка
"Телега @neji97"

